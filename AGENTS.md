# AGENTS.md

本文件只规定仓库内通用协作方式，不承载项目架构、市场规则、算法公式、求解器选择或模块归属。当前代码事实、未完成闭环和下一开发路线统一见 `docs/策略算法框架详细设计-v5.md` 的 §3、§14、§15；v3 为已批准架构决策，v0～v2 与 v4 为历史溯源。

## 1. Think Before Coding

- 实施前先核对实际代码、入口、配置和测试，不用 README 推断未验证行为。
- 明确目标、范围、假设和可验证的完成条件；存在多种高影响解释时先澄清。
- 历史文档只用于追溯，不得直接作为当前需求或验收依据。

## 2. Simplicity First

- 只实现已确认的需求，不增加推测性的功能、配置或抽象。
- 优先选择能够满足目标的最小改动。
- 单次使用的逻辑不为未来可能性提前设计扩展框架。

## 3. Surgical Changes

- 只修改与当前任务直接相关的文件和代码。
- 不顺手重构、格式化或删除无关内容。
- 保留用户已有改动；若发生重叠，先理解并兼容现有状态。
- 只清理由本次修改产生的未使用导入、变量或文件。

## 4. Goal-Driven Execution

- 将任务转换成可验证目标，并在实现后运行与风险相称的检查。
- 修复缺陷时先建立可复现证据；结构调整时验证调整前后的公共行为。
- 声明完成前检查实际 diff、测试结果和文档一致性。

## 5. 文档纪律

- `docs/策略算法框架详细设计-v5.md` 是唯一在研设计、当前事实快照和下一开发路线；其中标记为“待决策”的内容不构成实施授权。
- `docs/策略算法框架详细设计-v3.md` 只保留已批准的架构决策；v0～v2 与 v4 只作历史溯源。
- 代码、配置、入口、测试和 README 的事实声明必须一致。
- 修改公共行为、入口、配置或验证口径时，同步更新对应文档。
