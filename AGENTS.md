# AGENTS.md

本文件定义本仓库内 Agent Coding 的默认协作方式。
目标是让代理在 `ele-trading` 中优先服务“研究型原型 + 最小闭环跑通”，而不是偏离到生产化重构或无关扩展。

## 项目定位

- 这是一个独立 Python 研究型原型项目，不是生产系统。
- 当前首要目标是打通最小闭环：`数据 -> 预测 -> 场景 -> 优化 -> 控制 -> 评估 -> 应用入口`。
- 当前优先级高于一切的能力是：
  - 储能套利可运行
  - MPC 滚动优化可运行
  - Two-stage 骨架可导入、可实例化、可继续扩展
  - 最小回测可输出指标
- 默认优化栈是 `PuLP` 优先；`Pyomo` 当前主要用于 `Two-stage + CVaR` 的扩展骨架，不要过早把主要闭环迁移到 `Pyomo`。

## 沟通与语言

- 默认使用简体中文回复，除非用户明确要求英文。
- 语气直接、克制、少套话。
- 先执行，再汇报；除非真的阻塞，否则不要频繁反问用户。
- 进行较长任务时，给出简短进度更新：正在看什么、改什么、下一步做什么。
- 结尾优先汇报：
  - 做了什么
  - 如何验证
  - 还有什么风险或未覆盖范围

## 工作原则

- 先读上下文再动手，不要凭空假设模块职责或市场规则。
- 默认把用户请求理解为“希望直接落地并验证”，而不是只做方案讨论。
- 优先做最小修复和增量增强，不做无关重构，不顺手扩展成大而全架构。
- 如果发现实现与运行要求冲突：
  - 先做最小修复把链路跑通
  - 再评估是否需要补结构性整理
- 不要主动引入额外抽象层，除非它明显减少重复并且直接服务当前闭环。

## 仓库结构认知

- 应用入口在 `app/`
- 核心包在 `src/ele_trading/`
- 测试在 `tests/`
- 样例数据在 `data/`
- 配置在 `configs/`
- 文档在 `docs/`

当前 `src/ele_trading/` 的模块边界应尽量保持稳定：

- `data/`：样例数据、配置、读取逻辑
- `forecasting/`：预测接口与占位实现
- `scenario/`：场景采样与削减
- `optimization/`：套利、MPC、Two-stage 等优化模型
- `control/`：滚动执行与控制封装
- `evaluation/`：结算、指标、回测
- `utils/`：轻量工具能力

除非用户明确要求，否则不要重建目录结构，不要把现有模块重新拆分。

## 代码修改约束

- 搜索优先使用 `rg` / `rg --files`。
- 手工修改优先小步提交式改动，保持 diff 清晰。
- 不要覆盖、回滚或清理用户已有改动。
- 不要修改与当前任务无关的文件，只因为“顺手可以更整洁”。
- 注释保持稀少，只解释不直观的设计意图、约束或边界条件。
- 保持代码风格偏简单、可读、可运行；研究原型阶段优先清晰而不是过度抽象。

## 依赖与环境

- 默认 Python 版本要求以 `pyproject.toml` 为准：`>=3.11`。
- 默认使用项目根目录下的虚拟环境 `.venv`。
- 安装依赖优先使用：

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -e '.[dev]'
```

- 若 editable 安装出现构建问题，先检查 `pyproject.toml` 的 `build-system` 和基础打包依赖，而不是先怀疑业务代码。
- 未经必要性验证，不要随意新增重型依赖。

## 运行与验证顺序

完成改动后，优先按最小闭环顺序验证，而不是一上来全量扩展：

1. `./.venv/bin/python app/run_storage_arbitrage.py`
2. `./.venv/bin/python app/run_mpc_demo.py`
3. `./.venv/bin/python app/run_two_stage_skeleton.py`
4. `./.venv/bin/python app/run_backtest.py`
5. `./.venv/bin/pytest -q`

验证原则：

- 优先运行与改动最近的最小闭环命令。
- 如果只改了某个局部模块，也至少补一次与之最相关的入口或测试验证。
- 如果无法运行测试或脚本，要明确说明原因、影响范围和未验证部分。

## 业务语义约束

- `data/` 下样例数据是人工构造的最小示例，不代表真实市场数据。
- 代理不得把样例结果表述为真实市场收益、真实策略表现或生产可用结论。
- 当前结果主要用于：
  - 验证接口形状
  - 验证求解链路
  - 支撑后续扩展

## 关于优化模块的特别约束

- `storage_arbitrage.py` 和 `mpc_storage.py` 是当前最关键的可运行链路，修复优先级最高。
- `two_stage_cvar.py` 当前首先要保证：
  - 可导入
  - 可建模
  - 可实例化
  - 接口为后续扩展留好位置
- 在未被明确要求前，不要为了“完整性”强行补齐复杂风险建模、分解算法或多市场耦合。

## 测试策略

- 发现 bug 或行为缺失时，优先补最小失败用例，再修复实现。
- 测试应优先覆盖：
  - 样例数据加载
  - 储能套利求解输出结构
  - MPC 单窗口或滚动输出结构
  - Two-stage 骨架可构建
  - 最小回测指标输出
- 不要为了凑覆盖率添加大量低价值测试。

## 文档同步

- 当实现行为发生变化时，同步检查：
  - `README.md`
  - 对应模块目录下的 `README.md`
  - 相关 app 脚本的运行说明
- 文档应反映“当前实际可运行行为”，不要写尚未落地的能力像是已经完成。

## Git 约束

- 默认使用非交互式 git 命令。
- 未经用户明确要求，不要主动提交、推送、开 PR。
- 不要执行破坏性命令，如：
  - `git reset --hard`
  - `git checkout -- <file>`
  - 强制删除用户文件

## 默认交付格式

完成任务后，默认按以下顺序汇报：

1. 做了什么
2. 如何验证
3. 还有哪些风险、限制或下一步建议

