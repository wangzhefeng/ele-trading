# 配置模块说明

本目录存放市场、储能、场景等基础配置样例。

## 当前文件

- `market.yaml`：市场时间粒度、结算口径、价格字段等样例配置。
- `storage.yaml`：储能设备约束与效率参数。
- `scenario.yaml`：场景数量、扰动幅度和权重设置。

## 与上下游关系

- 上游来自业务规则与设备参数。
- 下游由 `data/loader.py`、`scenario/sampler.py`、`optimization/*` 使用。

## 扩展建议

后续可增加省份市场规则、偏差结算规则、多市场配置与资产分组配置。
