# 数据目录说明

本目录用于承接研究型原型中的输入数据、场景数据与回测说明。

## 子目录

- `raw/`：原始样例数据，适合直接被 demo 和测试读取。
- `processed/`：预留给特征处理和预测输出。
- `scenarios/`：多场景样例数据。
- `backtest/`：回测说明与结果占位目录。

## 当前策略

由于目标文档没有附带真实 CSV 或 JSON，本项目按变量语义构造最小示例数据集，用于保证样例和测试闭环能运行。
