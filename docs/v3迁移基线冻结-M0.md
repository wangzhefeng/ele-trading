# v3 迁移基线冻结记录（M0）

> 生成日期：2026-08-02
>
> 分支：`dev`（基线提交 `220d24d`，M0 改动未提交）
>
> 本文是 v3 迁移阶段 M0 的退出证据：回归基线冻结、事件契约补全、legacy 场景 API 弃用告警生效。后续每个迁移阶段完成后必须复核本文基线仍然成立。

## 1. 回归基线

执行命令（active-only 全集 + M0 新增 `tests/domain` + 完整入口冒烟）：

```bash
UV_CACHE_DIR=.uv_cache uv run python -m pytest -q \
  tests/domain tests/optimization tests/forecasting tests/scenario tests/data_provider \
  tests/utils tests/trading tests/positions tests/backtest tests/markets \
  tests/test_structure_layers.py tests/test_v2_phase1b_structure.py \
  tests/test_v2_phase2_contracts.py tests/test_yaml_config_loading.py \
  tests/test_entry_scripts.py
```

结果：`388 passed, 4 skipped, 3 deselected, 12 warnings in 54.76s`。

构成：原基线 366 项 + M0 新增事件契约测试 20 项 + 入口冒烟补全；`3 deselected` 为 `slow` 标记；`4 skipped` 为用户侧入口（cvxpy 可选依赖未安装）；12 项 warnings 含 legacy 场景 API 弃用告警（预期生效）。

## 2. M0 变更清单

| 变更 | 文件 | 说明 |
|---|---|---|
| 事件契约补全 | `src/ele_trading/domain/events.py` | 新增 `MarketCalendar`（市场代码、时区、交割粒度、交易日时段数校验）；`TradingEvent` 增加 `calendar`、`unit` 字段与 `delivery_period` 属性；全部字段带校验 |
| 事件导出 | `src/ele_trading/domain/__init__.py` | 导出 `MarketCalendar` |
| 事件测试 | `tests/domain/test_events.py` | 20 项：日历校验、事件校验、交割时段、全链子类可构造 |
| legacy 弃用告警 | `src/ele_trading/scenario/sampler.py` | `generate_price_scenarios` 发 `DeprecationWarning`（D-007） |
| legacy 弃用告警 | `src/ele_trading/scenario/reduction.py` | `normalize_weights` 与 `reduce_scenarios(list[PriceScenario])` 兼容路径发 `DeprecationWarning`；内部归一化拆为 `_normalize_weights` 避免重复告警 |

## 3. 公共 API 冻结清单

M0 之后各活动包 `__all__`（后续阶段的兼容面以此为准，字段只增不改）：

- `domain`：AwardEvent, BidEvent, DRCommitment, DecisionTrace, DispatchEvent, ForecastEvent, IntradayAdjustment, IntradayPlan, MarketCalendar, MarketForecastBundle, MeteringEvent, OperationalPlan, PositionState, SettlementEvent, TradingEvent
- `forecasting`：ForecastRequest, ForecastResult, ForecastProvider, ForecastModelRegistry 等 54 项（含价格/负荷/风光/天气模型与指标函数）
- `scenario`：canonical = Scenario, ScenarioSet, build_joint_scenarios, reduce_scenarios, ReductionDiagnostics；legacy（弃用期）= PriceScenario, generate_price_scenarios, normalize_weights
- `optimization`：BESSConfig, BESSVariables, add_bess_constraints, add_cvar_auxiliaries, solve_bess_arbitrage, run_bess_mpc, solve_one_mpc_window, build_two_stage_cvar_model, solve_two_stage_cvar, solve_pulp_model, SolveStatus, SolverResult 等 19 项
- `positions`：BidLadder, CorridorAdvice, PositionPlan, build_bid_ladder, build_position_corridor, plan_mid_long_position, rebalance_position_gap
- `operations`：solve_day_ahead_operational, solve_intraday_rolling
- `demand_response`：DRDecision, estimate_arbitrage_opportunity_cost, evaluate_dr_participation
- `markets`：single_settlement, dual_settlement（插件子包）
- `trading`：TradingOrchestrator, SampleTradingDataProvider, WalkForwardSeasonalNaiveProvider
- `backtest`：run_walk_forward_backtest, compute_extended_metrics, compute_rainflow_degradation, summarize_bess_metrics

## 4. 后续阶段退出复核点

每阶段合并前必须确认：

1. 上述测试命令结果保持全绿（允许新增测试，不允许删除或跳过既有项）；
2. legacy 场景 API 弃用告警持续生效，直至 D-007 删除条件达成；
3. §3 公共 API 清单的既有名称不被删除或改语义。
