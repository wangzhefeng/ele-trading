# 场景模块说明

场景模块负责把点预测扩展成多场景输入，服务于期望收益优化和风险建模。

## 当前文件

- `sampler.py`：基于预测值生成简单扰动场景。
- `reduction.py`：做权重归一化和简单场景削减。

## 上下游关系

- 上游依赖预测模块输出的点预测或分位数结果。
- 下游被 Two-stage 建模和回测流程消费。

## 扩展建议

后续可补充 Copula、Bootstrap、误差重采样、场景一致性校验和高级场景削减算法。
